# 기업분석 자동화 시스템 — 개발 작업계획

> 이 문서는 Claude Code, Codex CLI, Gemini CLI 등 **어떤 AI 코딩 도구로 이어서 작업하더라도** 동일하게 참조할 수 있는 단일 진행 문서입니다.
> 토큰/세션이 끊기면 다른 도구가 이 파일을 열어 체크박스 상태와 "진행 로그"만 보고 이어서 작업할 수 있어야 합니다.

- 작성일: 2026-08-17
- 원본 목업: `index.html` (정적 프로토타입, 하드코딩 데이터)
- 목표: 목업의 UI/흐름을 유지하면서 실제 PDF 파싱 → 데이터 저장 → 엑셀 생성 → 대시보드 반영이 동작하는 로컬 시스템 구축

---

## 0. 절대 원칙 (제약사항)

- **로컬 전용, LLM API 미사용.** 목업 하단 상태바에 반복 명시됨 — 파싱은 규칙/라벨 기반으로만 수행. 이 제약을 바꾸려면 사용자 확인 필요.
- PDF 형식은 **CRETOP / KODATA 기업종합보고서**로 한정.
- 기존 `index.html`의 화면 구조·디자인 토큰(색상, 레이아웃)은 최대한 유지하고, 하드코딩된 부분만 실데이터 연동으로 교체.

---

## 1. 기술 스택 (가정 — 확인 필요 ⚠️)

| 영역 | 선택 | 근거 |
|---|---|---|
| 백엔드 | Python 3.11+ / FastAPI + Uvicorn | 목업 로그에 `pdfplumber`, `openpyxl` 언급되어 Python 기반 암시. FastAPI는 로컬 단일 사용자 도구에 가볍고 자동 문서화(`/docs`) 제공 |
| PDF 텍스트 추출 | ~~pdfplumber~~ → **PyMuPDF (`fitz`)** (2026-08-17 변경, 근거는 5번 로그 참고) | 실제 샘플 PDF에서 pdfplumber는 한글이 전부 깨진 문자(mojibake)로 추출됨. PyMuPDF는 동일 파일에서 정상 추출 확인 |
| 신용등급 등 이미지 값 | `pytesseract` + Tesseract-OCR (로컬 실행, API 아님) | 기업신용등급/EW등급/기업성장등급이 텍스트가 아니라 게이지 이미지로 렌더링됨(5번 로그 참고). 시스템에 Tesseract-OCR 바이너리 별도 설치 필요 — **사용자 설치 확인 필요** |
| 엑셀 생성 | `openpyxl` | 목업 로그에 명시됨 |
| 데이터 저장 | 회사별 JSON 파일 (`data/{사업자번호}.json`) | 목업 로그의 `JSON 저장 → data/옥산농원.json`과 일치. 규모(~128개사) 상 DB 불필요, 추후 검색 성능 이슈 시 SQLite 인덱스 추가 검토 |
| 프론트엔드 | 기존 `index.html`(바닐라 HTML/CSS/JS) 유지, `fetch()`로 API 연동 | 프레임워크 도입은 이 도구의 규모에 비해 과함 |

> Tesseract-OCR 설치 및 검증 완료 (아래 5번 로그 참고). 스택 전체 실제 PDF로 검증 완료.

---

## 2. 폴더 구조 (목표)

```
20260817_강수찬 영업 지원 프로젝트/
├── PLAN.md                      # 이 파일
├── README.md                    # 설치/실행 방법
├── requirements.txt
├── .gitignore
├── backend/
│   ├── app.py                   # FastAPI 진입점
│   ├── models.py                # pydantic 데이터 스키마
│   ├── storage.py                # JSON 저장/조회
│   ├── parser/
│   │   ├── labels.py            # 라벨 앵커 · 정규식 정의
│   │   ├── pdf_parser.py        # PyMuPDF 기반 텍스트/필드 파싱 로직
│   │   └── grade_ocr.py         # 신용등급 등 게이지 이미지 OCR (pytesseract)
│   ├── excel/
│   │   └── generator.py         # openpyxl 기반 엑셀 생성
│   └── routers/
│       ├── companies.py
│       ├── upload.py
│       └── validation.py
├── data/                         # 회사별 파싱 결과 JSON
├── uploads/                      # 원본 PDF 보관
├── outputs/                      # 생성된 엑셀 파일
├── frontend/
│   └── index.html                # 기존 목업 → 실데이터 연동판
└── tests/
    └── sample_pdfs/               # 테스트용 실제 PDF 샘플
```

---

## 3. 단계별 작업 (Phase)

체크박스는 완료 시 `- [x]`로 바꾸고, 담당 도구/날짜를 뒤에 `(Claude, 2026-08-18)` 형식으로 덧붙입니다.

### Phase 0 — 프로젝트 기반 설정
- [x] git 저장소 초기화 + `.gitignore` (venv, data/, uploads/, outputs/ 등 제외) (Claude, 2026-08-17)
- [x] 폴더 구조 생성 (backend/, data/, uploads/, outputs/, frontend/, tests/) (Claude, 2026-08-17)
- [x] `requirements.txt` 작성 (fastapi, uvicorn, pdfplumber, openpyxl, python-multipart, pydantic) (Claude, 2026-08-17)
- [x] 기존 `index.html` → `frontend/index.html`로 이동 (Claude, 2026-08-17)
- [x] `README.md` 초안 (설치/실행 방법) (Claude, 2026-08-17)

### Phase 1 — 샘플 PDF 확보 & 라벨 규칙 정의
- [x] 실제 CRETOP/KODATA 기업종합보고서 PDF 65개 확보 확인 — 프로젝트 루트 `PDF/` 폴더에 사용자가 전량 보관 중. 기준 샘플로 `1 옥산농원.pdf`를 `tests/sample_pdfs/1_옥산농원.pdf`로 복사해 분석 (Claude, 2026-08-17)
- [x] PDF 구조 분석 완료 (아래 "PDF 구조 분석 결과" 참고) (Claude, 2026-08-17)
- [x] 전체 추출 필드 목록 1차 확정 (아래 "필드 매핑표" 참고) — 나머지 64개 파일도 동일 양식(KOREA RATING & DATA/KCB 발급, 총 31페이지 구조)이라는 사용자 확인 받음 (Claude, 2026-08-17)
- [ ] 라벨 앵커 · 정규식 정의 (`backend/parser/labels.py`) — Phase 2에서 코드로 구현
- [ ] (권장) 다른 유형 샘플 1~2개 추가 확인 — 페이지 수/재무제표 유무가 회사마다 다를 수 있음 (예: 옥산농원은 현금흐름표·자본변동표가 "조회된 자료가 없습니다"). 파서가 이런 결측 섹션을 정상 처리하는지 Phase 2/7에서 검증 필요

#### PDF 구조 분석 결과 (`1 옥산농원.pdf`, 31페이지, KOREA RATING & DATA 발급)

- **텍스트 추출 라이브러리 이슈**: `pdfplumber`(pdfminer 기반)로 추출 시 한글이 전부 `�` mojibake로 깨짐 — 이 PDF들이 쓰는 임베드 폰트가 pdfminer의 CID→유니코드 매핑과 맞지 않는 것으로 보임. **`PyMuPDF`(`fitz`)는 동일 파일에서 완벽하게 정상 추출** → 텍스트 추출은 PyMuPDF로 확정.
- **표는 "표"가 아니라 순차 텍스트 스트림**: 재무제표 등 표 데이터가 셀 구조로 안 잡히고 `라벨` → `2023값` → `2024값` → `2025값` 순서의 평문 텍스트로 쭉 이어짐(좌표 기반 정렬 아님). 파서는 라벨 뒤에 오는 N개의 숫자 토큰을 연도 순서대로 매칭하는 방식으로 구현 필요. 결측 연도는 `-`로 표시됨.
- **신용등급/EW등급/기업성장등급은 텍스트가 아니라 이미지(게이지 그래픽)**: 2페이지에 게이지 3개가 이미지로 삽입되어 있고(`기업신용등급` 예: "bb+", `EW등급` 예: "정상", `기업성장등급`은 이 샘플에선 값 없음 "?"). 텍스트 레이어에는 라벨만 있고 값이 없음 → **OCR 필요** (`pytesseract` + Tesseract-OCR, 로컬 실행이라 "LLM API 미사용" 원칙에 위배되지 않음). 게이지 이미지는 페이지 내에서 크기(약 318×159, 129×124 등)로 다른 로고 이미지와 구분 가능.
- **재무진단 5축(성장성/수익성/재무구조/부채상환능력/활동성)**: 28~31페이지에 서술형 의견 텍스트로 존재하고, 각 페이지 끝에 `"...은/는 {등급}함"` 형태의 짧은 요약이 별도 텍스트 조각으로 존재 (예: `성장역량은양호함`, `수익창출능력은우수함`, `재무안정성은양호함`, `부채상환능력은우수함`, `재무효율성은우수함`). 정규식 `(양호|우수|보통|미흡|취약)함$` 로 등급 단어만 추출 가능. **목업 상세 페이지의 5각 다이얼과 정확히 일치.**
- **금액 단위 이중 표기**: 2~6페이지 요약 섹션은 **백만원** 단위(목업 KPI가 쓰는 단위와 동일), 12~21페이지 상세 재무제표는 **천원** 단위. 대시보드/상세페이지 KPI는 요약 섹션(4~6p) 값을 우선 사용하고, "재무상태표 요약" 표 등 상세 수치가 필요하면 12~21p를 천원→백만원 환산해서 사용.
- **표지(1p) + 상세(2p) 이중 기재**: 기업명·사업자번호·대표자가 1페이지(표지)와 2페이지(상세)에 각각 나와 목업의 "표지 + 상세 2곳 교차검증" 규칙과 일치 — 둘이 다르면 검증 대기열로.
- **10페이지 "업계순위" 표에 동종업계 상대 데이터가 이미 포함됨** — 목업 상세페이지의 "동종업계 79개사 중 74위", "업종 평균/상위 25%" 비교 데이터의 출처가 바로 이 표(및 11페이지 "동종업계내경영규모비교"). 매출액 기준 자동 순위 계산 불필요, PDF에 이미 계산되어 있음.

#### 필드 매핑표 (1차, Phase 2에서 `labels.py`로 코드화)

| 필드 | 페이지 | 라벨/패턴 | 비고 |
|---|---|---|---|
| 기업명 | 1, 2 | `기업명` | 표지+상세 교차검증 |
| 사업자번호 | 1, 2 | `사업자번호` | 정규식 `\d{3}-\d{2}-\d{5}`, 표지+상세 교차검증 |
| 대표자명 | 1, 2, 7 | `대표자` / `대표자명` | |
| 주소 | 2 | `주소` | |
| 설립년월 | 2 | `설립년월` | `YYYY-MM-DD` |
| 업종 | 2 | `표준산업분류(11차)` | 괄호 안 코드 + 업종명, 예: `(A01231)양계업` |
| 기업유형/기업규모 | 2 | `기업유형` / `기업규모` | 예: 개인사업자 / 소기업 |
| 기업신용등급 | 2 (이미지) | 게이지 이미지 OCR | 예: `bb+` |
| EW등급 | 2 (이미지) | 게이지 이미지 OCR | 예: `정상` |
| 기업성장등급 | 2 (이미지) | 게이지 이미지 OCR | 값 없을 수 있음(`?`) |
| 요약 재무 3개년 (매출/영업이익/순이익 등) | 5 | `요약손익계산서` 섹션 | 단위 백만원, 목업 KPI와 동일 단위 |
| 재무상태표 요약 (자산/부채/자본총계) | 4 | `요약재무상태표` | 단위 백만원, 목업 "재무상태표 요약" 표와 일치 |
| 상세 재무상태표/손익계산서 | 12~16 | 계정명 순차 라벨 | 단위 천원 |
| 재무비율 (부채비율, ROE 등) | 6, 22~27 | `요약재무비율` 및 상세 `재무비율` | 단위 % |
| 재무진단 5축 등급 | 28~31 | `...함` 문장 종결 패턴 | 정규식 `(양호|우수|보통|미흡|취약)함$` |
| 업계순위 | 10 | `업계순위` 표 | 목업 "동종업계 N개사 중 M위" 출처 |
| 동종업계 비교 | 11 | `동종업계내경영규모비교` 표 | 목업 "업종 평균/상위25%" 출처 |

### Phase 2 — PDF 파싱 엔진
- [x] PyMuPDF 텍스트 추출 모듈 (`backend/parser/pdf_parser.py`) (Claude, 2026-08-17)
- [x] 라벨 기반 기본정보 필드 매칭 (표지+상세 교차검증 포함) (Claude, 2026-08-17)
- [x] 재무상태표·손익계산서·재무비율 3개년 순차 텍스트 파싱 (라벨→N개 숫자 토큰 매칭, 결측 `-` 처리) (Claude, 2026-08-17)
- [x] 업계순위(10p)·동종업계 비교(11p) 파싱 (Claude, 2026-08-17)
- [x] 게이지 이미지 OCR 모듈 작성 및 검증 완료 (`backend/parser/grade_ocr.py`, 신용등급/EW등급/성장등급) — Tesseract-OCR 설치(winget) + 한국어 데이터 프로젝트 로컬 설치(`setup_tessdata.py`), 색상 기반 이진화 전처리로 게이지 아치와 텍스트 분리 후 OCR. 옥산농원 샘플 기준 `bb+`/`정상`/`None`(값없음) 전부 정확히 인식 (Claude, 2026-08-17)
- [x] 재무진단 5축 등급 추출 (`...함` 패턴 정규식) (Claude, 2026-08-17)
- [x] 파싱 실패/누락 필드 기초 감지 (`missing_fields`, `cross_check_mismatch`) — 검증 대기열 큐 자체는 Phase 3(저장)에서 연결 (Claude, 2026-08-17)
- [x] `tests/sample_pdfs/1_옥산농원.pdf` 기준 파싱 정확도 테스트 — `tests/test_pdf_parser.py`(pytest) 6개 항목 전부 통과, 목업 상세페이지 하드코딩 값과 100% 일치 확인 (기업명/대표자/주소/업종/재무3개년/재무진단5축/업계순위/동종업계비교) (Claude, 2026-08-17)
- [x] 구조가 다른 두 번째 샘플(법인 `(주)삼진지.에프`, 36p, 옥산농원과 페이지 수 다름)로 검증 — **중요 버그 발견 및 수정**: 아래 로그 참고 (Claude, 2026-08-17)
- [ ] 세 번째 이상 샘플 추가 검증 권장 (특히 현금흐름표 등 결측 섹션 있는 파일) — `PDF/` 폴더의 나머지 63개 중에서
- [ ] 기업성장등급이 실제로 값(한글 등급)을 가진 샘플로 `_read_korean_grade`의 한글 인식 정확도 검증 — 지금까지 확인한 2개 샘플 모두 성장등급이 "값 없음(?)" 상태라 한글 등급 인식 자체는 아직 미검증
- [ ] "흑자전환/적자전환" 같은 텍스트형 증가율 처리 — 아래 로그의 알려진 제한사항 참고 (현재 대시보드 UI에서 쓰지 않는 필드라 우선순위 낮음)

### Phase 3 — 데이터 저장 & 스키마
- [x] 회사 데이터 pydantic 모델 확정 (`backend/models.py`: `Company`, `DiagnosisRatings`, `IndustryRank`, `ValidationIssue`) (Claude, 2026-08-17)
- [x] `data/{사업자번호}.json` 저장/조회 함수 (`backend/storage.py`: `save_company`/`load_company`/`list_companies`) — 목업 로그는 회사명으로 파일명을 썼지만(`data/옥산농원.json`) 특수문자·중복 위험이 있어 사업자번호로 변경 (Claude, 2026-08-17)
- [x] 검증 이슈 판정 로직 (`_build_issues`): 필드 누락, 표지/상세 교차검증 불일치(형식 의심), 대표자명 숫자 포함(형식 의심), 재무제표 요약 누락, 동일 사업자번호 재조회(중복 의심 — `report_query_datetime` 비교) (Claude, 2026-08-17)
- [x] `report_query_datetime`/`evaluation_date`/`settlement_date` 파서에 추가 (중복 의심 판정용) (Claude, 2026-08-17)
- [x] `tests/test_storage.py` 5종 — 실사용 시나리오 단위 테스트, 전부 통과 (Claude, 2026-08-17)
- [x] 실제 PDF 2개로 전체 파이프라인(파싱→OCR→저장) 통합 스모크 테스트 — `data/{사업자번호}.json` 정상 생성 확인 (Claude, 2026-08-17)

### Phase 4 — 엑셀 자동 생성
- [x] openpyxl 워크북 생성 (`backend/excel/generator.py`) — 시트 3개: 요약(기본정보+등급+재무진단5축+업계순위), 재무제표(재무상태표/손익계산서/재무비율 3개년), 업계비교(동종업계 경영규모 비교) (Claude, 2026-08-17)
- [x] `Company` → 엑셀 매핑 함수 (`generate_excel`) (Claude, 2026-08-17)
- [x] `outputs/{기업명}_기업종합보고서.xlsx` 생성 확인 — 실제 저장된 회사 2건으로 생성 후 셀 값 검증 완료, 파일명 특수문자 무해화(`_sanitize_filename`) 처리 (Claude, 2026-08-17)
- [x] `tests/test_excel_generator.py` 4종 (총 21개 테스트 전부 통과) (Claude, 2026-08-17)

### Phase 5 — 백엔드 API
- [x] FastAPI 앱/라우터 골격 (`backend/app.py`, `backend/routers/{companies,upload,validation}.py`) — `/`는 `frontend/index.html`을 그대로 서빙 (Claude, 2026-08-17)
- [x] `POST /api/upload` (PDF 업로드 → 파싱 → OCR → JSON 저장 → 엑셀 생성, OCR 실패해도 텍스트 데이터는 등록되도록 방어) (Claude, 2026-08-17)
- [x] `GET /api/companies` (검색 `q`/업종 필터/페이지네이션) (Claude, 2026-08-17)
- [x] `GET /api/companies/{business_no}` (상세) (Claude, 2026-08-17)
- [x] `GET /api/companies/{business_no}/excel` (다운로드, 요청 시 즉석 재생성) (Claude, 2026-08-17)
- [x] `GET /api/issues` (검증 대기열) (Claude, 2026-08-17)
- [x] `PATCH /api/companies/{business_no}` (수동 수정 — 수정한 필드와 연관된 이슈 자동 해제) (Claude, 2026-08-17)
- [x] `GET /api/dashboard/summary` (등록수/파싱성공률/대기이슈/업종별/신용등급 구간별/최근등록) (Claude, 2026-08-17)
- [x] `tests/test_api.py` 8종(총 29개 테스트 통과) + 실제 PDF로 `POST /api/upload`→전체 API 흐름 수동 스모크 테스트 완료 (Claude, 2026-08-17)

### Phase 6 — 프론트엔드 실데이터 연동
- [x] 대시보드 하드코딩 데이터 → `fetch()` API 연동 (KPI, 업종별 바차트, 신용등급 도넛, 최근 등록 기업 테이블 전부 `/api/dashboard/summary` 기반) (Claude, 2026-08-17)
- [x] 기업 목록 페이지 검색/필터(업종·신용등급구간·매출액구간)/페이지네이션 실연동, 전체 엑셀 내보내기 (Claude, 2026-08-17)
- [x] 업로드 드롭존 → 실제 드래그앤드롭/파일선택 업로드(`POST /api/upload`) + 큐 진행 상태 + 실시간 처리 로그 + 등록 결과 미리보기 (Claude, 2026-08-17)
- [x] 기업 상세 페이지 완전 동적화 — 이전엔 옥산농원 하드코딩, 이제 어떤 회사든 `viewDetail(business_no)`로 조회. KPI 전년대비 증감률(매출/영업이익/순이익은 상대%, 부채비율은 %p 차이로 구분), 재무진단 5링(우수/양호/보통/미흡/취약 색상 매핑 CSS 추가), 3개년 추이·업종비교 막대, 재무상태표 표, 엑셀 다운로드·원본 PDF 보기 버튼(`GET /api/companies/{id}/source-pdf` 신규) (Claude, 2026-08-17)
- [x] 검증 대기열 화면 → `/api/issues` 연동, 이슈 유형별 카드(필드누락/형식의심/중복의심), "직접 수정"은 `prompt()` 기반 인라인 수정 → PATCH → 이슈 자동 해제 (Claude, 2026-08-17)
- [x] 사이드바 "기업 상세" 항목을 처음 조회 전까지 숨김 처리 후 조회한 회사명으로 동적 갱신, "데이터 검증" 배지 실시간 갱신 (Claude, 2026-08-17)
- [x] **실제 브라우저로 전 페이지 동작 검증** (Playwright 설치 후 headless Chromium으로 uvicorn 서버 구동 → 대시보드/목록/검색/상세/업로드(실제 PDF)/검증(이슈 발생→직접수정→해제) 전체 플로우 스크린샷 + 콘솔 에러 확인, 에러 0건) (Claude, 2026-08-17)

### Phase 7 — 테스트 & 품질 검증
- [x] 실제 PDF로 End-to-End 테스트 (업로드→파싱→엑셀→대시보드) — Phase 2/3/4/5/6 진행 중 반복 수행 (Claude, 2026-08-17)
- [x] **`PDF/` 폴더의 실제 65개 전부로 배치 테스트** — `parse_pdf`+`extract_grades`+`build_company` 파이프라인, 예외 0건/65건 (Claude, 2026-08-17)
- [x] 브라우저 수동 전체 플로우 테스트 (Playwright, Phase 6에서 완료 — 대시보드/목록/검색/상세/업로드/검증 5개 화면, 실제 PDF 업로드 + 이슈 수정까지, 콘솔 에러 0건) (Claude, 2026-08-17)
- [x] 파싱 실패/불확실 케이스 → 검증 대기열 정상 등록 확인 — 아래 배치 테스트로 발견한 OCR 이슈 2건을 수정하고 재검증 완료 (Claude, 2026-08-17)

#### 65개 전수 배치 테스트로 발견·수정한 이슈 (Phase 7)

1. **신용등급 OCR 오인식**: 색상 아치 잔여 노이즈가 섞여 65건 중 13건이 `rs)`, `bt`, `Db`, `dd-` 같은 잘못된 값으로 조용히 저장되고 있었음(검증 이슈로도 안 잡힘 — 가장 나쁜 실패 유형). **수정**: 등급 형식 정규식(`^(aaa|aa|a|bbb|bb|b|ccc|cc|c|d)[+\-0]?$`) 검증 추가 + 여러 PSM(6/7/8/11) 모드로 재시도, 그래도 안 맞으면 `None` + `missing_field`(`credit_grade`) 검증 이슈 등록. 결과: 65건 중 60건 정상 인식(형식 검증 통과), 5건만 `None`+이슈 등록(원본 PDF 확인 필요) — 잘못된 값이 조용히 저장되는 케이스 0건으로 개선.
2. **EW등급 OCR에 잡음 글자 혼입**: 이진화 잔여물이 실제 값 앞에 별도 줄로 붙어(`"가\n유보"`) 실제 값은 "유보"인데 첫 줄 "가"만 저장되던 버그(65건 중 8건 영향, 회귀 테스트로는 못 잡힘 — 두 샘플 모두 "정상"만 있어 이 버그 자체를 몰랐음). **수정**: OCR 결과에서 가장 긴 한글 연속 구간을 추출하고 2자 미만이면 노이즈로 버림. 결과: `정상` 55건 + `유보` 7건(수정 전엔 안 보이던 값) = 62/65 정상 인식.
3. `31 하늘팜.pdf`는 재무진단 5축이 전부 비어있었는데 확인해보니 파서 버그가 아니라 **PDF 원본 자체에 "재무정보가 부족합니다"라고 명시**되어 있는 정상적인 결측 케이스 (버그 아님, 그대로 둠).

#### 남은 낮은 우선순위 이슈
- EW등급 65건 중 1건(`과시`)은 여전히 의심스러운 값 — 화이트리스트 검증까지는 아직 안 넣음 (신용등급만큼 핵심적이지 않아 우선순위 낮춤)
- 기업성장등급은 65건 전부 `None`("값없음" 게이지) — 실제 PDF 이미지로 재확인해보니 파서가 아니라 원본 데이터 자체가 이 필드를 거의 안 채운다는 게 확인됨(정상 동작)
- "순이익증가율"의 "흑자전환/적자전환" 텍스트 케이스(Phase 2 로그 참고)는 여전히 미해결, 대시보드가 안 쓰는 필드라 우선순위 낮음

### Phase 8 — 실행/배포 준비
- [x] 로컬 실행 스크립트 `run.bat` — 처음 실행 시 venv/패키지/한국어 OCR 데이터 자동 준비 후 서버 기동 + 브라우저 자동 오픈. **주의**: 배치 파일에 한글 문구를 넣었더니 `chcp 65001`을 넣어도 cmd.exe가 파일을 여전히 시스템 코드페이지로 읽어 깨진 토큰을 명령어로 오인, 에러 메시지가 줄줄이 뜨는 문제 발생(서버 자체는 정상 기동하지만 보기엔 고장난 것처럼 보임) → **영문 메시지로 전량 교체**해 해결. 실제로 PowerShell로 두 번 구동해 HTTP 200 확인 (Claude, 2026-08-17)
- [x] README 정리 (빠른 시작/수동 설치/실행/사용 방법/폴더 구조/테스트) (Claude, 2026-08-17)
- [x] 사용자 매뉴얼 — 별도 문서 대신 README.md "사용 방법" 절로 충분하다고 판단(내부 소규모 도구라 과한 문서화 지양) (Claude, 2026-08-17)

---

## 4. 협업 규칙 (Claude / Codex / Gemini 병행 진행)

1. 작업 시작 전 이 파일을 열어 체크박스 상태를 확인 — 이미 진행 중이거나 완료된 항목 중복 작업 금지.
2. 완료한 항목은 즉시 `- [x] ... (도구명, 날짜)`로 갱신.
3. 진행 중 발견한 이슈, 방향 전환, 사용자 결정 사항은 아래 "5. 결정 사항 & 이슈"에 append.
4. 가능하면 **Phase 단위로 순차 진행** — 여러 도구가 동시에 같은 Phase/같은 파일을 건드리지 않도록 조율(각자 시작 전 이 파일의 최신 상태를 다시 읽을 것).
5. git 저장소가 준비되면(Phase 0) 도구별로 작업 후 커밋 — 커밋 메시지에 어떤 Phase 항목을 처리했는지 명시.

---

## 5. 결정 사항 & 이슈 로그

> 새 항목은 맨 아래에 날짜순으로 추가.

- 2026-08-17 (Claude): 초기 작업계획 수립. 기술 스택은 목업 코드(pdfplumber/openpyxl 언급)에서 유추한 가정이며 사용자 확정 필요.
- 2026-08-17 (Claude): 사용자가 기술 스택 확정, Phase 0 진행 승인. git init, 폴더 구조, requirements.txt, README.md, index.html→frontend/ 이동 완료. Phase 1(샘플 PDF 확보)부터는 사용자의 실제 PDF 샘플이 필요.
- 2026-08-17 (Claude): 사용자가 프로젝트 루트 `PDF/` 폴더에 실제 변환 대상 PDF 65개를 이미 넣어둠(양계업 1개 + 농업회사법인/영농조합법인 다수). "모두 동일한 구조"라는 확인을 받아 `1 옥산농원.pdf` 1개를 기준으로 Phase 1 구조 분석 수행, `tests/sample_pdfs/1_옥산농원.pdf`로 복사. **중요 스택 변경 2건**: (1) pdfplumber → PyMuPDF(fitz) 텍스트 추출 라이브러리 교체(pdfplumber는 이 PDF들의 폰트에서 한글이 전부 깨짐, 근거는 1번 표). (2) 신용등급/EW등급/기업성장등급이 이미지(게이지)로 렌더링되어 있어 OCR(pytesseract+Tesseract-OCR) 단계 신규 추가 — **사용자가 Tesseract-OCR을 시스템에 설치해야 함** (README.md에 안내 추가, 현재 이 머신엔 미설치 확인됨). requirements.txt/venv 갱신 완료(pymupdf, pytesseract 설치·pdfplumber 제거). 상세 필드 매핑표는 Phase 1 섹션 참고.
- 2026-08-17 (Claude): Phase 2 파서 코드 작성 완료. `backend/parser/{labels,pdf_parser,grade_ocr}.py`. 텍스트 기반 필드(기본정보/재무3개년/재무비율/재무진단5축/업계순위/동종업계비교)는 `tests/test_pdf_parser.py`로 회귀 테스트 작성, 옥산농원 샘플로 6개 테스트 전부 통과 — 목업 상세페이지 하드코딩 값과 100% 일치. `venv`에 pytest 추가.
- 2026-08-17 (Claude): 사용자 승인 하에 Tesseract-OCR을 winget(`UB-Mannheim.TesseractOCR`)으로 설치. 관리자 권한이 없어 설치 폴더(`C:\Program Files\Tesseract-OCR\tessdata`)에 한국어 언어 데이터(`kor.traineddata`)를 쓸 수 없었음 → 대신 프로젝트 로컬 `.tessdata/`에 내려받아 `TESSDATA_PREFIX`로 사용하는 방식으로 우회(`setup_tessdata.py` 신규, `.gitignore`에 `/.tessdata/` 추가, 용량 커서 git 미추적). 게이지 이미지를 그대로 OCR하면 색깔 아치가 섞여 오인식이 심해(`fbb+`, `asvan` 등) 색상 기반 이진화(무채색+어두운 픽셀만 검정으로 남김) 전처리를 추가, `bb+`/`정상`/`None`(값없음) 전부 정확히 인식하도록 개선. `tests/test_pdf_parser.py`에 `test_grade_ocr` 추가(Tesseract 없으면 스킵), 총 7개 테스트 전부 통과.
- 2026-08-17 (Claude): 사용자 요청으로 구조가 다른 두 번째 샘플(`10 (주)삼진 지.에프.pdf`, 36페이지, 일반법인)로 검증 진행. **버그 발견**: 업계순위(10p→13p)/동종업계비교(11p→14p)/재무진단(28~31p→34~36p)가 옥산농원(31p, 개인사업자)과 다른 페이지에 위치해 고정 페이지 인덱스로 찾던 기존 구현이 전부 빈 값을 반환함. **수정**: `parse_pdf`가 문서 전체 페이지를 하나의 줄 리스트로 이어붙여(`all_lines`) 헤더 텍스트를 검색하도록 변경 — 페이지 위치와 무관하게 동작. 요약재무3표(4/5/6p)는 두 샘플 모두 같은 위치라 안정적으로 확인됨. `tests/test_pdf_parser_corp.py` 신규 추가(페이지-무관성 회귀 테스트), 총 12개 테스트 전부 통과. **알려진 제한사항 발견**: 순이익이 흑자/적자를 오가면(예: 578→-29→580) "순이익증가율"이 숫자 대신 "흑자전환"/"적자전환" 텍스트로 표기되어 현재 파서는 해당 필드를 조용히 건너뜀(에러는 아님) — 대시보드가 이 필드를 안 쓰므로 우선순위 낮음, 추후 Excel에 전체 재무비율을 담을 때 처리 필요.
- 2026-08-17 (Claude): Phase 3 완료. `backend/models.py`(pydantic `Company`/`DiagnosisRatings`/`IndustryRank`/`ValidationIssue`), `backend/storage.py`(저장/조회/이슈판정) 작성. 저장 키를 목업 로그의 회사명(`data/옥산농원.json`) 대신 사업자번호로 변경 — 특수문자·동명 회사 위험 회피(의도적 이탈, 목업 로그 문구와 다름). 중복 의심 판정용으로 `report_query_datetime`/`evaluation_date`/`settlement_date` 파서에 추가. `tests/test_storage.py` 5개 신규(총 17개 테스트 전부 통과), 실제 PDF 2개로 파싱→OCR→저장 전체 파이프라인 통합 스모크 테스트도 확인(`data/{사업자번호}.json` 정상 생성).
- 2026-08-17 (Claude): Phase 4 완료. `backend/excel/generator.py` — 요약/재무제표/업계비교 3개 시트 워크북 생성. 엑셀 파일명은 (저장 키와 달리) 목업 로그 문구 그대로 회사명 사용(`outputs/{기업명}_기업종합보고서.xlsx`) — 다운로드용 파일명이라 사업자번호보다 회사명이 사용자 친화적이라고 판단, Windows 금지문자만 무해화 처리. 실제 저장된 회사 데이터로 생성해 openpyxl로 다시 읽어 셀 값 검증 완료. `tests/test_excel_generator.py` 4개 신규(총 21개 테스트 전부 통과).
- 2026-08-17 (Claude): Phase 5 완료. `backend/app.py` + `backend/routers/{companies,upload,validation}.py`. `models.py`에 `CompanyUpdate`/`CompanyListItem`/`CompanyList`/`IssueEntry`/`DashboardSummary` 추가, `storage.py`에 `update_company`(PATCH용, 수정 필드와 연관된 이슈 자동 제거)/`latest_revenue`/`grade_band`(대시보드 신용등급 구간, 목업 도넛 범례 A~BBB/BB/B/CCC 이하와 동일) 추가. `httpx` 추가(FastAPI TestClient용). `tests/test_api.py` 8개(총 29개 테스트 통과) + `TestClient`로 실제 PDF 업로드→전체 API 흐름(루트 HTML 서빙 포함) 수동 스모크 테스트 완료. 다음 Phase 6는 `frontend/index.html`의 하드코딩 데이터를 이 API들로 교체하는 작업.
- 2026-08-17 (Claude): Phase 6 착수 전 API 보강 — `GET /api/companies`에 `grade_band`/`revenue_min`/`revenue_max` 필터, `GET /api/companies/{id}/source-pdf`(원본 PDF 보기 버튼용), `CompanyListItem`에 `operating_profit_latest`/`diagnosis_summary`(5축 중 최저 등급, `storage.overall_diagnosis`) 추가. 테스트 32개로 증가.
- 2026-08-17 (Claude): **Phase 6 완료.** `frontend/index.html`의 `<script>`를 전면 재작성 — 대시보드/목록/업로드/상세/검증 5개 화면 전부 하드코딩 데이터 대신 `/api/*` 호출로 렌더링. 사업자번호 기반 라우팅(`viewDetail(business_no)`)으로 상세 페이지를 임의 회사에 대해 동작하도록 일반화(기존엔 옥산농원 고정). CSS에 재무진단 등급별 링 색상(`.diag-ring.fair/.poor/.na`) 추가. 검증 페이지는 이슈 유형별 카드 + `prompt()` 인라인 수정 → PATCH로 이슈 해제. 실행/검증: Playwright(Chromium)를 이 머신에 새로 설치(`playwright install chromium`, ~300MB)해 실제 `uvicorn backend.app:app`을 띄우고 5개 화면 전부 스크린샷 확인 — 검색/필터/실제 PDF 업로드/검증 카드 수정→해제까지 실제 상호작용으로 검증, 콘솔 에러 0건. (사소한 버그 1건 발견 즉시 수정: 업로드 로그 창에 placeholder 문구가 안 지워지고 첫 로그 줄과 이어붙던 것.) chromium-cli 등 프로젝트 전용 실행 스킬은 없었음 — 다음에 유사 작업 시 `/run-skill-generator`로 이번 설치 과정을 스킬화하는 것을 권장.
- 2026-08-17 (Claude): **Phase 8 완료 — Phase 0~8 전부 완료.** `run.bat`(Windows 더블클릭 실행, 최초 실행 시 venv/패키지/한국어 OCR 데이터 자동 준비 + 서버 기동 + 브라우저 자동 오픈), README.md 전면 정리(빠른 시작/수동 설치/사용 방법/폴더 구조/테스트 절 추가). **주의**: 배치 파일에 한글 안내문을 넣었더니 `chcp 65001`을 넣어도 cmd.exe가 여전히 시스템 코드페이지로 파싱해 깨진 토큰을 명령어로 오인하는 에러가 줄줄이 뜸(서버 자체는 정상 기동하지만 고장난 것처럼 보임) → 영문 메시지로 전량 교체해 해결, PowerShell로 두 번 재구동해 HTTP 200 확인. 이걸로 계획서의 모든 단계가 끝났습니다. 남은 건 각 Phase 로그에 적어둔 "낮은 우선순위" 항목들(성장등급 한글 인식 미검증, 흑자전환/적자전환 텍스트 처리, EW등급 잔여 오인식 1건 등)과 나머지 PDF들을 실제로 하나씩 업로드해보는 정도 — 전부 선택 사항이고, 핵심 파이프라인(PDF 업로드→파싱→OCR→저장→엑셀→대시보드→검증)은 실사용 가능한 상태.
- 2026-08-17 (Claude): 사용자가 실사용 중 `3 한원푸드시스템(주).pdf` 업로드에서 "Failed to fetch" 오류 보고(파싱 로그가 "PDF → 텍스트 추출…" 찍힌 지 2초 뒤 에러). **재현 시도**: 동일 파일로 (1) 파서 함수 직접 호출, (2) httpx로 서버에 직접 업로드, (3) `--reload` 옵션 켠 서버에 업로드(mid-request 재시작 의심), (4) Playwright 실제 브라우저로 드롭존 업로드 — 전부 정상 처리됨(200 OK), 재현 실패. uvicorn 0.52.3의 `--reload` 기본 include 패턴이 `*.py`뿐이라 uploads/data/outputs 쓰기로는 재시작 안 됨을 소스 확인으로 배제. **원인 미확정** (안티바이러스의 신규 파일 실시간 잠금, 일시적 네트워크 문제 등 이 세션에서 재현 못 하는 환경적 요인 추정). **방어적 보강은 진행**: `backend/routers/upload.py`에서 기존엔 `parse_pdf`/`extract_grades`만 try/except로 감쌌었는데, 파일 저장(`dest.write_bytes`)과 `build_company`/`save_company`/`generate_excel` 구간은 무방비였음 — 전부 try/except로 감싸 어떤 예외든 크래시 대신 깔끔한 JSON 오류 응답을 반환하도록 수정, 파일명도 `Path(...).name`으로 경로조작 방지. `tests/test_api.py`에 업로드 엔드포인트 테스트 3개 신규(비-PDF 거부, 손상된 PDF가 크래시 아닌 422 반환 확인, 실제 PDF 전체 흐름) — 이전엔 업로드 엔드포인트 테스트가 전혀 없었던 공백이었음. 총 36개 테스트 통과. **사용자에게 재시도 요청 + 서버 터미널 창에 에러가 떴었는지 확인 요청.**
- 2026-08-17 (Claude): 위 버그 조사 후속 — 사용자가 "일부 실패해도 나머지 정보가 맞으면 그냥 등록되게 하자"는 방향으로 요청. `backend/parser/pdf_parser.py`의 `parse_pdf`를 섹션별 장애 격리 구조로 리팩터링: 기존엔 표지/재무상태표/손익계산서/재무비율/업계순위/동종업계비교/재무진단 중 한 곳이라도 예외를 던지면 함수 전체가 실패해 업로드가 통째로 거부됐음(예: 재무상태표는 정상 추출됐어도 업계순위 파싱에서 예외 나면 전부 버려짐) → `_safe()` 헬퍼로 각 섹션을 개별 try/except로 감싸 한 섹션이 실패해도 나머지는 계속 추출, 실패한 섹션 이름과 예외 내용을 `ParsedCompany.parse_errors`에 기록. `storage._build_issues`가 이걸 `missing_field` 검증 이슈로 변환해 검증 대기열에 노출(원본 PDF 확인 유도). `doc.close()`는 `finally`로 이동해 예외 경로에서도 확실히 정리. 회귀 테스트 2개 추가(파서 단에서 강제로 예외 주입해 나머지 필드는 정상 추출되는지, 스토리지 단에서 회사가 정상 등록되고 이슈만 뜨는지) — 이 리팩터링으로 `3 한원푸드시스템(주).pdf` 문제가 실제로 해결됐는지는 확인 못 함(재현 자체를 못 했으므로), 다만 "부분 실패시 통째로 실패" 클래스의 문제는 구조적으로 막았음. 65개 전체 배치 재실행, 예외 0/65 유지. 총 38개 테스트 통과.
- 2026-08-17 (Claude): 사용자 요청으로 `프로그램 시작.bat` 추가 — 비기술 사용자가 더블클릭할 진입점 이름을 더 직관적으로. **버그 발견**: 처음엔 `run.bat`을 중복 작성하지 않으려고 `프로그램 시작.bat`이 `cd /d "%~dp0"` 후 `call run.bat`으로 위임하게 만들었는데, PowerShell `Start-Process`로 띄워보니 "'run.bat'은(는) 내부 또는 외부 명령이 아닙니다" 에러 — `run.bat` 파일이 실제로 그 폴더에 존재하고(`dir` 출력으로 확인) `%~dp0`도 정상 해석되는데도(`cd` 자체는 성공) `call run.bat`이 실패함. 원인은 끝내 특정 못 함(이 프로젝트 경로에 `&`+한글+공백이 섞여 있어 cmd.exe 파싱이 어딘가 꼬이는 것으로 추정, `echo %~dp0`처럼 따옴표 없이 `&` 포함 경로를 확장하면 `&`가 명령 구분자로 오인되는 건 확인했지만 `call run.bat` 라인 자체엔 그 패턴이 없어 완전히 설명은 안 됨). **해결**: 위임 대신 `run.bat`의 내용을 `프로그램 시작.bat`에 그대로 복제(자기완결형) — 실제로 안정적으로 동작함을 재확인(PowerShell로 재기동, HTTP 200). 두 파일이 의도적으로 중복된 상태이므로 실행 로직을 고칠 땐 **양쪽 다** 수정해야 함(양쪽 파일 상단에 주석으로 명시해둠). README.md에도 두 파일이 동일하다고 안내 추가.
- 2026-08-17 (Claude): **"우편발송용 목록" 기능 신규 구현.** 사용자가 `참고자료/우편발송용 목록(샘플).xlsx`(No./우편번호/주소/상호명/대표자 성명 컬럼)를 참고자료로 제공, 왼쪽 메뉴에 동일 기능 추가 요청.

  **중요 데이터 품질 버그 발견 및 수정**: 참고자료와 우리 파서의 주소를 대조하다가, PDF의 '주소' 필드가 전남 지역 회사인데도 "광주"로 잘못 표기되는 경우를 발견함(예: 옥산농원 실제 주소는 "전남 나주시…"인데 PDF엔 "광주나주시…"로 나옴 — 나주시는 전라남도 소속이지 광주광역시가 아님). 65개 전부에 대해 파서 결과 vs 참고자료를 정량 대조한 결과 65개 중 62개에서 동일 패턴 재현. 실제 광주광역시 주소(대표자 거주지 등에 등장, 구 이름이 뒤따름)는 건드리면 안 되므로, "광주" 뒤에 동/서/남/북구/광산구가 오지 않을 때만 "전남"으로 교정하는 규칙을 `backend/parser/pdf_parser.py`의 `_normalize_address()`에 구현(`labels.py`에 `GWANGJU_DISTRICTS` 상수 추가). 우편번호도 주소 문자열에서 분리해 `Company.postal_code` 별도 필드로 저장(`ParsedCompany`/`Company`/`CompanyUpdate`에 필드 추가). 참고자료와 재대조 결과 65개 중 64개 정확히 일치(나머지 1개 "가족식품"은 PDF 내 주소 자체가 참고자료와 다른 위치를 가리키는 별개 이슈 — 데이터 자체가 다름, 정상화 로직 문제 아님).

  **API/엑셀**: `backend/routers/mailing.py` 신규 — `GET /api/mailing-list`(우편번호순 정렬 JSON), `GET /api/mailing-list/excel`(참고자료와 동일한 시트명·컬럼 구조로 다운로드, `backend/excel/generator.py`의 `generate_mailing_list_excel()`). `app.py`에 라우터 등록.

  **프론트엔드**: 사이드바 "기업 목록" 바로 아래에 "우편발송용 목록"(📮) 메뉴 추가, 새 페이지(`mailingPage`)에 No./우편번호/주소/상호명/대표자 성명 테이블 + 엑셀 다운로드 버튼.

  **기존 저장 데이터 백필**: 이 기능을 만들기 전, 사용자가 이미 실사용 중 65개 중 62개를 직접 업로드해서 `data/`에 실제 데이터가 쌓여 있었음(파일 타임스탬프·순차 처리 간격으로 볼 때 사용자가 드롭존에 여러 파일을 한번에 끌어다 놓은 것으로 추정 — 삭제하지 않고 보존). 이 데이터들은 주소 정규화 수정 이전에 파싱된 것이라 우편번호가 없고 주소에 "광주" 오류가 남아있었음 → 일회성 백필 스크립트로 기존 저장분 64건 전부 재정규화(다른 필드는 그대로 유지), 참고자료와 재대조해 64/64 정확히 일치 확인.

  테스트: `tests/test_address_normalization.py`(정규화 로직 단위 테스트 4개, 샘플 PDF 없어도 항상 실행), `tests/test_mailing_list.py`(API+엑셀 형식 검증 2개), `tests/test_pdf_parser.py`에 주소/우편번호 검증 추가. 총 45개 테스트 통과. Playwright로 실제 브라우저에서 페이지 진입→목록 렌더링(64행)→엑셀 다운로드까지 확인, 콘솔 에러 0건, 다운로드한 엑셀이 참고자료와 동일한 시트명/컬럼 구조임을 재확인.

- 2026-08-17 (Claude): **"PDF 전체 다 뽑아달라" 요청 — 대규모 파서 확장.** 사용자가 지금까지 추출한 항목이 원본 PDF(31페이지) 대비 일부(요약 재무제표/등급/재무진단 등급 단어 정도)뿐이라 지적, 페이지별 전수 점검 후 누락분을 정리해 보고, 전체를 다 뽑기로 결정.

  **신규 추출 항목** (`backend/parser/pdf_parser.py`/`labels.py`, `ParsedCompany`/`Company`에 필드 추가):
  - `credit_info`: 신용정보 10개 플래그(행정처분/신용도판단/공공정보/상거래연체/당좌개설카드발급/단기연체/당좌거래정지/휴폐업/법인등기/회생절차) — 라벨 뒤 자유 텍스트를 다음 라벨 전까지 이어붙이는 방식
  - `certifications`(벤처/이노비즈/메인비즈/연구소 등 5개), `ip_rights`(특허/실용신안/디자인/상표권 4개) — 표지 페이지와 동일한 "N라벨 후 N값" 패턴 재사용
  - `relationship_existence`: 주요주주/관계회사/주요구매처/주요판매처 존재 여부
  - `ledger_detail`: **재무상태표/손익계산서/현금흐름표/자본변동표/이익잉여금처분계산서/제조원가명세서 전체 계정과목** — 기존 요약표(4~12개 항목)와 달리 "라벨 뒤 숫자 3개가 이어지는 모든 줄"을 훑는 범용 스캐너(`_scan_labeled_number_triples`)로 회사당 80~275개 계정 항목 추출. 회사마다 결측 섹션(예: 현금흐름표 자료없음)이 있어 실제 존재하는 표만 포함
  - `ratio_detail`: **재무비율 상세 80~140여 개** — 성장성/수익성/안정성/활동성/생산성 카테고리로 그룹화(기존 요약표는 12개뿐)
  - `diagnosis_commentary`: 재무진단 5축 서술 전체 문장(기존엔 "양호"/"우수" 등급 단어만 저장했음)
  - `personal_info`: 대표자 성명/직위, 생년월일/성별
  - `soft_sections`: 연혁/사업목적/종합의견/경영진현황/주식소유현황/주요주주현황/관계회사현황/사업장현황/구매처현황/판매처현황/매출구성 — 표 구조가 회사마다 크게 달라 완벽한 컬럼 구조화 대신 원문 텍스트 보존("조회된 자료가 없습니다"만 있으면 `None`)

  **버그 2건 발견 및 수정**: (1) "재무상태표"/"손익계산서" 헤더가 3페이지 MY재무Data 요약표의 열 제목으로 먼저 등장해 첫 매치 검색으로는 상세표(12/15p) 대신 요약표 열 제목을 잘못 찾음 → 상세표 뒤엔 항상 "단위:천원"이 바로 이어지는 것으로 진짜 위치를 구분(`_find_ledger_header`). (2) 자유 텍스트 구간을 이어붙일 때 페이지 머리말(조회일시/COPYRIGHT/쪽번호)이 섞여 들어감 → `_is_page_furniture()` 필터 추가.

  **엑셀**: `backend/excel/generator.py`에 시트 4개 추가 — "재무제표(상세)"(전체 계정과목), "재무비율(상세)"(카테고리별), "신용정보・인증", "기타정보"(인적사항/분석의견 전문/연혁 등). 데이터 있을 때만 상세 시트 추가.

  **검증**: 옥산농원(31p)·삼진지에프(36p) 두 샘플로 필드별 회귀 테스트 8개 신규(총 52개 테스트 통과). `PDF/` 폴더 65개 전체로 파싱 배치 테스트 — 예외 0건, 재무제표 상세 평균 155개 항목/회사, 재무비율 상세 평균 135개 항목/회사(유일한 0건은 이미 알려진 "재무정보 부족" 케이스인 하늘팜, 버그 아님). 65개 전체로 파싱→저장→엑셀생성 전체 파이프라인도 예외 0건 확인.

  **기존 저장 데이터 재백필**: `data/`에 이미 저장돼 있던 64개 회사(이전 대화에서 사용자가 직접 업로드한 실데이터, PLAN.md 우편발송 로그 참고)는 확장 전 파서로 저장된 것이라 새 필드가 비어있었음 — `source_pdf` 경로로 원본을 다시 찾아 새 파서로 재파싱해 64개 전부 갱신(주소/우편번호 등 기존 값도 그대로 유지됨, 실패 0건). 서버 재기동 후 대시보드/목록/상세/엑셀다운로드/우편발송목록 API 전부 200 확인, Playwright로 상세 페이지 재확인(콘솔 에러 0건) — 별도 재업로드 없이 바로 반영됨.

- 2026-08-18 (Claude): **"추출된 내용 전체가 기업별 상세 화면에서 제공되었으면 좋겠어" — 확장 추출 필드를 프론트엔드 상세 화면에 노출.** 직전 항목에서 백엔드/엑셀엔 이미 반영됐지만 프론트엔드 상세 페이지는 여전히 기존 요약 섹션(1~4번)만 보여주고 있었음 — `credit_info`/`certifications`/`ip_rights`/`ledger_detail`/`ratio_detail`/`diagnosis_commentary`/`personal_info`/`relationship_existence`/`soft_sections` 9개 필드가 API 응답엔 있지만 화면엔 전혀 안 나오는 상태였음.

  **`frontend/index.html`에 섹션 5~9번 추가**: 5. 재무진단 분석의견(전문 텍스트, 데이터 없으면 섹션 자체 숨김), 6. 신용정보・인증 현황(신용정보/기업인증/산업재산권 3열 카드), 7. 재무제표 상세(6개 재무표별 접이식 패널, 표당 계정과목 전체를 연도별 표로), 8. 재무비율 상세(성장성/수익성/안정성/활동성/생산성 5개 카테고리별 접이식 패널), 9. 기업 상세 정보(대표자 인적사항/관계회사 존재여부/11개 소프트 섹션을 접이식 패널로, 원문 텍스트 그대로 표시). 재무제표・재무비율은 항목 수가 회사당 80~275개로 많아서 목업 디자인 언어에 맞는 카드형 `<details>/<summary>` 접이식 패널(`.dtl` CSS 클래스, 화살표 회전 애니메이션)로 구현해 기본은 접힌 상태(항목 개수만 표시)로 화면이 압도되지 않게 함.

  신규 JS 함수 8개(`renderDiagnosisCommentary`/`renderCreditInfo`/`_yearlyDetailTable`/`_detailsPanel`/`renderLedgerDetail`/`renderRatioDetail`/`_kvBlockHtml`/`renderOtherInfo`)를 `loadDetail()`에 연결. 백엔드/모델은 변경 없음 — 순수 프론트엔드 작업.

  **검증**: `pytest tests/ -q` 52개 전부 통과(백엔드 무변경 확인용). 서버 기동 후 Playwright로 옥산농원 상세 페이지 실제 렌더링 확인 — 신용정보 카드 3개, 재무제표 상세 패널 3개(재무상태표 51개 계정/손익계산서 31개/제조원가명세서 25개), 재무비율 상세 패널 5개, 기타정보 패널 13개, 재무진단 분석의견 전문 정상 표시, 콘솔/페이지 에러 0건. 재무상태표 패널을 펼쳐 전체 계정과목표(자산(*)/유동자산(*)/당좌자산(*) 등, 3개년 값)가 세션 초반에 검증한 원본 PDF 수치와 정확히 일치함을 스크린샷 2장(접힌 상태/펼친 상태)으로 재확인. 커밋 `000b822`.

- 2026-08-18 (Claude): **"기업목록 화면은 '검색조회 목록.xlsx'로 목록을 구현해줘" — 기업목록을 영업 대상 마스터 리스트 기반으로 전면 전환.** `참고자료/검색조회 목록.xlsx`(gitignore 대상, 실제 사업자 정보)를 열어보니 "전체DB" 시트에 756개 영업 대상 후보(기업체명/대표자명/형태/업종/법인등록번호만 채워짐, 주소·연락처·매출액·기업등급은 거의 공란)와 "유한회사(91)" 시트(91개, 수작업으로 조사된 상세정보 — 이번 작업 범위 밖, 건드리지 않음)가 있었음. 기존 "기업목록" 화면은 PDF 분석 완료된 64개만 보여주고 있어 정체가 완전히 다른 두 목록이었음 — AskUserQuestion으로 방향을 확인, 사용자가 **"757개 전체를 마스터 목록으로 표시"** 선택(분석완료 기업은 배지+실데이터, 미분석은 xlsx 기본정보만 + PDF 업로드 유도).

  **매칭 문제**: xlsx의 "법인등록번호"는 우리 시스템의 "사업자번호"와 형식이 다른 별개 식별자(법인등록번호 vs 사업자등록번호)라 ID로 매칭 불가 — 회사명 정규화 매칭으로 해결. 단순 공백제거만으로는 실제 파싱된 64개 중 41개(64%)만 매칭됐는데, "농업회사법인"/"영농조합법인"/"유한회사"/"㈜"/"(주)" 등 법인형태 표기가 출처마다 위치·스타일이 달라 나머지가 누락됐음 — 이 노이즈 단어들을 제거하고 비교하는 `normalize_name()`으로 46개(72%)까지 개선, **오매칭(서로 다른 두 회사가 매칭되는 경우) 0건** 확인 후 채택(실측 스크립트로 ambiguous count 검증). 나머지 18개는 정규화로도 못 잡는 게 아니라 애초에 이 xlsx 목록에 존재하지 않는 회사(예: 옥산농원/하늘팜 — 개발 중 사용한 테스트 샘플 PDF라 영업 대상 리스트엔 없음, 정상).

  **백엔드**: `backend/master_list.py` 신규 — xlsx "전체DB" 시트 로더(`load_master_rows`, mtime 기반 캐시) + `normalize_name()`. `backend/routers/master_list.py` 신규 — `GET /api/master-list`(q/industry/grade_band/revenue_min/revenue_max/analyzed_only 필터 + 페이지네이션, `storage.list_companies()`와 정규화 이름으로 조인), `GET /api/master-list/industries`(xlsx 전체 업종 목록, 필터 드롭다운용). `models.py`에 `MasterListItem`/`MasterList` 추가. 기존 `/api/companies`는 상세조회/PATCH/엑셀다운로드용으로 그대로 유지(용도가 다름 — 이건 저장된 회사 자체의 CRUD, master-list는 xlsx 조인 조회).

  **프론트엔드**: `frontend/index.html`의 기업목록 표를 xlsx 컬럼 기준(No/기업체명/대표자명/업종/법인등록번호/상태/신용등급/매출액)으로 재구성, 상태 컬럼에 분석완료(초록 배지)/미분석(회색 배지) 표시, 미분석 행은 "상세" 버튼 대신 "PDF 업로드" 버튼으로 업로드 화면 유도. 업종 필터 드롭다운은 `/api/master-list/industries`(75개 업종, 마스터리스트 전체 기준)로 교체(기존엔 분석완료 64개 기준 대시보드 요약에서 가져왔었음). 전체 엑셀 내보내기는 `analyzed_only=true`로 필터링해 분석완료 기업만 대상으로 동작(미분석은 애초에 엑셀 생성할 데이터가 없음).

  **검증**: `tests/test_master_list.py` 6개 신규(정규화 로직 단위테스트, tmp_path에 만든 가짜 xlsx로 매칭/analyzed_only 필터/검색/업종목록 API 테스트, 실제 xlsx 파일 의존 없이 항상 실행됨) — 총 57개 테스트 통과. 실제 xlsx(756행)로 서버 기동 후 확인: `/api/master-list` 정상 응답(총 756, analyzed_only=true는 46개), `/api/master-list/industries` 75개 업종. Playwright로 실제 브라우저 검증 — 목록 페이지 1(756개 중 1-10 표시, 분석완료/미분석 배지 정상), 업종 필터 76개 옵션 로드, 마스터리스트에 없는 회사("옥산농원") 검색 시 빈 결과 정상 표시, 매칭되는 회사("새롬한방제약") 검색 시 분석완료 배지+상세보기 버튼 정상 동작해 상세 페이지까지 이동 확인, 콘솔/페이지 에러 0건. 커밋 `520d9c0`.

- 2026-08-18 (Claude): **"추가메뉴(개인사업자 일반법인 농업회사법인 영농조합법인).xlsx로 4개 메뉴 만들어줘" — 법인형태별 분류 메뉴 4개 신규 추가.** 참고자료를 열어보니 시트 4개(개인사업자 106행/일반법인 261행/농업회사법인 219행/영농조합법인 108행, 총 694행) + 이전에 이미 쓴 "유한회사(91)" 시트가 있었음(이번엔 4개만 요청받아 나머지는 건드리지 않음). 직전 작업(기업목록 마스터리스트, `520d9c0`)과 달리 이 4개 시트는 이미 대표자 연령/기업등급/주채권은행/매출액/영업이익/당기순이익/보험료/배당유무 등 20개 컬럼이 전부 조사되어 채워진 상태라, PDF 분석 여부와 무관하게 그 자체로 바로 목록에 쓸 수 있는 데이터였음.

  **매칭 로직 재사용 + 확장**: 개인사업자 시트의 "사업자 번호" 컬럼은 우리 시스템의 `business_no`와 형식이 동일(사업자등록번호)해서 직접 매칭 가능했지만, 나머지 3개 시트(일반법인/농업회사법인/영농조합법인)는 "법인등록번호" 컬럼이라 여전히 별개 식별자 — 기존 마스터리스트 기능에서 만든 `master_list.normalize_name()`을 그대로 재사용해 이름 매칭. 코드 중복 없이 기존 정규화 로직을 그대로 가져다 쓴 것.

  **백엔드**: `backend/category_list.py` 신규 — 카테고리 키(individual/general_corp/agri_corp/farm_partnership) -> 시트명 매핑 + mtime 캐시 로더(`load_category_rows`). 4개 시트의 컬럼 위치(A~T, 20열)는 완전히 동일하고 헤더 문구만 미세하게 다름(예: 개인사업자만 "사업자 번호", 나머지는 "법인등록번호"; "법인세 감면" vs "법인세감면" 등) — 헤더 텍스트가 아니라 컬럼 위치로 파싱해 이 차이를 흡수. `backend/routers/category_list.py` 신규 — `GET /api/category-list/{category}`(검색/analyzed_only 필터/페이지네이션, 존재하지 않는 카테고리는 404), `GET /api/category-list/{category}/excel`(원본 20개 컬럼을 그대로 재수출 — 화면엔 요약 컬럼만 보이지만 엑셀은 전체 데이터 보존). `backend/excel/generator.py`에 `generate_category_excel()` 추가. `models.py`에 `CategoryListItem`/`CategoryList` 추가(참고자료 원본 값은 `credit_grade_ref`/`revenue_ref`로 분리 보존 — 우리 파싱 데이터가 있으면 `credit_grade`/`revenue_latest`가 우선 표시되지만 원본 값도 잃지 않음).

  **프론트엔드**: 사이드바에 "영업 대상 분류" 그룹 신설, 4개 메뉴 추가. 4개가 컬럼 구조와 동작이 완전히 같아서 페이지 하나(`categoryPage`)를 `currentCategory` 파라미터로 재사용(기존 `기업 상세` 페이지가 `currentBusinessNo`로 재사용되는 것과 동일한 패턴) — 메뉴 전환 시 검색어/페이지 상태 초기화. 표는 No/기업체명/대표자명/업종/주소/상태/기업등급/매출액 컬럼, 상태가 "분석완료"면 실제 신용등급(색상 배지)+상세보기 버튼, 아니면 참고자료 원본 등급(회색 배지, 형식이 제각각이라 색상 판정 없이 원문 그대로)+PDF 업로드 버튼.

  **검증**: `tests/test_category_list.py` 6개 신규(가짜 xlsx로 사업자번호 매칭/이름 정규화 매칭/빈 시트/404/검색/엑셀 원본컬럼 보존 테스트, 실제 xlsx 없이 항상 실행됨) — 총 63개 테스트 통과. 실제 xlsx(694행)로 서버 기동 후 4개 API 전부 정상 응답(개인 2/261법인 17/농업 18/영농 10개씩 매칭 확인). Playwright 실브라우저 검증 — 4개 메뉴 전환마다 제목·총건수 정상(106/261/219/108), 검색("덕성"->"덕성종돈 영농조합법인") 정상, 일반법인에서 매칭된 회사("현대도정공장") 검색 후 상세보기 클릭 시 실제 상세 페이지로 정상 이동 확인, 콘솔 에러 0건. 커밋 `3c0e4e4`.

- 2026-08-18 (Claude): **"본 프로젝트를 내 GitHub에 배포해줘" → GitHub 공개 저장소 생성 + Render.com 배포 인프라 준비.** 1단계: `gh repo create`로 `leehee4343/corp-analysis-automation` 저장소 생성(Public, 사용자가 AskUserQuestion으로 직접 선택 — 비공개 권장했으나 공개로 결정), 기존 커밋 전부 푸시. 푸시 전 `git ls-files`로 민감 데이터(PDF/데이터/참고자료 xlsx) 추적 여부 재확인 — 전부 `.gitkeep`만 있고 실데이터는 없음을 확인 후 진행.

  2단계: 사용자가 "GitHub URL은 소스코드 저장소일 뿐 접속 가능한 웹사이트가 아니다"를 인지하고 실제 인터넷 배포를 요청. AskUserQuestion으로 호스팅 플랫폼(Render.com 선택)과 인증 여부(**"일단 인증 없이 배포" 선택** — 로그인 없이 URL만 알면 실제 기업 재무데이터에 누구나 접근 가능하다는 위험을 명시적으로 고지했음에도 사용자가 인증 없이 진행하기로 결정, 존중하여 진행. **추후 실사용 전에는 최소 비밀번호 게이트 추가를 다시 권장할 것**).

  **배포 인프라 구현**: `Dockerfile`(Tesseract-OCR + 한국어 언어팩 apt 설치, uvicorn 기동, `$PORT` 환경변수 사용 — Render가 동적으로 포트 지정), `.dockerignore`, `render.yaml`(Render Blueprint — 영구 디스크를 `/app/storage`에 마운트, `plan: starter`로 지정, 월 $7 수준부터 유료). **저장 경로 리팩터링**: 기존엔 `backend/storage.py`/`backend/routers/upload.py`/`backend/excel/generator.py` 3곳에 각각 `Path(__file__).resolve().parents[N] / "data"` 식으로 하드코딩돼 있어 클라우드 컨테이너 재배포·재시작마다 데이터가 초기화될 위험이 있었음 → `backend/paths.py` 신규 — `APP_STORAGE_ROOT` 환경변수가 설정되면 그 아래에 data/uploads/outputs를 두고, 미설정(로컬 개발 기본값)이면 기존과 완전히 동일하게 프로젝트 루트 기준 상대경로 사용. 각 모듈은 이제 이 상수를 import만 함 — 로컬 동작 무변화, 63개 테스트 전부 통과 확인 후 커밋(`c3a988f`).

  **알려진 제약사항 (사용자에게 고지 필요)**: `참고자료/` 폴더(검색조회 목록.xlsx, 추가메뉴.xlsx — 기업목록 마스터리스트·법인형태별 4개 메뉴가 쓰는 실제 사업자 데이터)는 민감정보라 처음부터 gitignore 대상이었음 → **배포된 서버 컨테이너에는 이 파일들이 없어서, 그 5개 메뉴(기업목록/개인사업자/일반법인/농업회사법인/영농조합법인)는 배포 직후엔 전부 빈 목록으로 뜬다.** 코드 자체는 `if not path.exists(): return []`로 방어되어 있어 크래시는 안 나지만, 실사용하려면 이 파일들을 서버에 별도로 올리는 방법(예: Render Shell로 직접 업로드, 또는 관리자용 업로드 엔드포인트 추가)을 이후에 논의해야 함 — 이번 작업 범위에는 포함 안 함.

  **남은 수동 단계**: Render 계정 생성·GitHub 연동은 브라우저 기반 작업이라 Claude가 대신 할 수 없음 — 사용자에게 안내만 제공, 완료 후 실제 URL을 받아 최종 확인 예정.

- 2026-08-18 (Claude): **Render 배포 완료.** 사용자가 "그냥 니가 해줘"라고 요청 — Render 계정/GitHub 연동 자체는 브라우저 인증이 필요해 대신 할 수 없다고 설명하고, 대신 사용자가 Render API 키만 발급받아 전달하면 나머지(서비스 생성·배포·확인)는 API로 전부 처리하겠다고 제안, 사용자가 동의하고 키 전달.

  `render.yaml`(영구 디스크+`starter` 유료 플랜) 기준으로 첫 시도했으나 Render API가 402(결제수단 필요)로 거부 — AskUserQuestion으로 "카드 등록 후 유료로" vs "무료 플랜(데이터 미보존)"을 물었고, 사용자가 처음엔 카드 등록 쪽을 택했다가 곧 "무료로는 안 되냐" 재질문 → 무료 플랜의 정확한 트레이드오프(15분 미사용 시 자동 절전, 재시작/재배포마다 업로드 데이터 전부 초기화)를 다시 명확히 고지한 뒤 최종적으로 **무료 플랜(데모/테스트용)으로 진행 확정**.

  Render API(`POST /v1/services`, `type=web_service`, `env=docker`, `plan=free`, 디스크 없음)로 서비스 생성 → 자동으로 GitHub 저장소 빌드(Dockerfile 기반, Tesseract-OCR 설치 포함) 시작 → 백그라운드 폴링 스크립트로 배포 상태 감시 → `live` 확인 후 실제 URL에 curl로 루트 페이지(200)와 `/api/dashboard/summary`(정상 JSON, 빈 데이터 — 프레시 컨테이너라 예상된 상태) 응답 검증 완료.

  **서비스 URL**: `https://corp-analysis-automation.onrender.com` (무료 플랜, 영구 디스크 없음 — 데모/기능 확인용, 실 업무 데이터는 계속 로컬에서 관리 권장). `배포용 URL.txt`에 GitHub 저장소 URL과 함께 기록, 무료 플랜의 데이터 미보존 주의사항도 명시. 이 파일은 git 미추적(로컬 참고용).

- 2026-08-18 (Claude): **배포 서버에 참고자료 반영 + 배포 환경용 로그인 추가.** 배포 직후 사용자가 "기업목록에 '기업 목록'이 노출되지 않아"라고 문의 — 배포 서버 컨테이너에는 `참고자료/*.xlsx`(gitignore 대상)가 없어 마스터리스트/법인형태별 메뉴가 빈 목록으로 뜨는 것이 원인(에러 아님, 정상 동작). Render 무료 플랜은 영구 디스크가 없어 배포 시점에 파일을 미리 넣어둘 방법이 없음 — `backend/routers/admin.py` 신규(`POST /api/admin/reference-upload/{master_list|category_list}`)로 배포된 서버에 참고자료를 직접 전송해 반영할 수 있게 함. `ADMIN_UPLOAD_TOKEN` 환경변수와 대조하는 별도 토큰으로 보호(토큰 미설정 시 항상 거부). `tests/test_admin.py` 4개 신규.

  실제 업로드를 시도하자 시스템 권한 분류기가 차단 — 사유: **지금 배포된 사이트는 로그인이 전혀 없는 공개 URL**이라, 실제 사업자 데이터(대표자명·매출액·신용등급 포함)를 올리면 URL만 아는 누구나 조회 가능해지는 상태였음. 사용자에게 재확인(AskUserQuestion) 후 "간단한 비밀번호 로그인을 먼저 추가"하기로 결정. `backend/auth_middleware.py` 신규(`BasicAuthMiddleware`) — `APP_LOGIN_PASSWORD` 환경변수가 설정된 경우에만 앱 전체(프론트엔드+API)에 HTTP Basic 인증을 강제, 미설정 시(로컬 개발 기본값) 기존처럼 인증 없이 동작해 `run.bat` 워크플로우에 영향 없음. `tests/test_auth_middleware.py` 5개 신규. Render 환경변수(`APP_LOGIN_USER=admin`, `APP_LOGIN_PASSWORD=1234` — 사용자가 직접 지정)로 활성화, 배포 확인 결과 최초 반영 실패(env var 변경만으로는 이미 떠 있던 프로세스에 반영 안 됨 — Render API로 강제 재배포 트리거해서 해결) 후 정상 동작 확인(무인증/오답 401, 정답 200).

  인증이 걸린 것을 확인한 뒤 참고자료 2개 파일(`검색조회 목록.xlsx`, `추가메뉴...xlsx`)을 실제로 업로드 완료.

  **중대한 사고 발생 및 원인**: 업로드 직후 로컬/배포 양쪽에서 "기업목록에 불러오기 실패: 요청 실패(500)" 확인 — 원인을 추적한 결과, **`backend/routers/admin.py`의 초기 버전이 대상 경로를 모듈 임포트 시점에 딕셔너리로 미리 캡처해두는 구조**(`_TARGETS = {"master_list": master_list.MASTER_LIST_PATH, ...}`)였는데, 이 값은 테스트의 `monkeypatch.setattr(master_list, "MASTER_LIST_PATH", tmp_path/...)`가 적용되기 *전에* 이미 고정돼버린 상태였음. `tests/test_admin.py`의 업로드 테스트가 실행되면서, 테스트용 더미 엑셀("dummy" 한 글자만 든 파일)이 격리된 tmp_path가 아니라 **실제 로컬 `참고자료/검색조회 목록.xlsx` 파일에 그대로 덮어써짐** — 실데이터 손실 사고. `_target_path()` 함수로 바꿔 요청 시점에 `master_list.MASTER_LIST_PATH`를 매번 새로 읽도록 수정해 재발은 막았지만, 이미 손상된 원본 파일은 로컬에 다른 사본이 없고 Windows 이전 버전 복구도 불가해 **복구하지 못함** — 사용자에게 즉시 원인과 결과를 있는 그대로 보고. (다른 파일 `추가메뉴...xlsx`/`우편발송용 목록(샘플).xlsx`는 영향 없음.) **교훈**: 실제 파일 경로를 다루는 코드는 절대 모듈 임포트 시점에 값을 캡처하지 말고 호출 시점에 참조해야 함 — `backend/storage.py`의 `_get_conn()`(아래 SQLite 마이그레이션)에도 이 교훈을 명시적으로 반영함.

- 2026-08-18 (Claude): **회사 데이터 저장소를 JSON 파일 → SQLite로 마이그레이션.** 위 사고를 계기로 사용자가 "이걸 지속가능하게 쓰려면 데이터베이스 연결이 좋겠다"고 제안, Claude도 동의(파일 기반 저장은 경로 버그 한 번에 실데이터가 통째로 날아갈 수 있음을 방금 직접 겪음). Plan 모드로 진입해 설계 확정 후 진행.

  **범위**: `data/{사업자번호}.json` → `data/companies.db`(SQLite, 표준 라이브러리 `sqlite3`만 사용, 새 의존성 없음) 하나로. `참고자료/*.xlsx`(마스터리스트·법인형태별 메뉴), `uploads/*.pdf`, `outputs/*.xlsx`는 이번 범위 밖(원래도 파일로 두는 게 자연스러움) — `storage.py`가 다루는 "회사 분석 결과 레코드"만 이관.

  **조사로 확인한 사실(설계 근거)**: `storage.py`에서 실제 파일시스템을 건드리는 함수는 `save_company`/`load_company`/`list_companies` 3개뿐이고, 나머지(`_build_issues`/`build_company`/`update_company`/`list_issues`/`latest_value`/`latest_revenue`/`overall_diagnosis`/`grade_band`)는 이미 로드된 `Company` 객체만 다루거나 저 3개를 호출만 해서 **코드 변경이 전혀 필요 없음**. `companies.py`/`upload.py`/`validation.py`/`mailing.py`/`master_list.py`/`category_list.py` 등 모든 라우터는 `storage.*`가 돌려주는 `Company` 객체만 쓰므로 **라우터 코드도 한 줄도 안 바뀜**. 필터링/정렬(업종/신용등급/매출액 구간, 최신순)은 이미 파이썬에서 하고 있어 이번엔 그대로 유지(SQL 푸시다운은 회사 수가 훨씬 늘어났을 때의 별도 최적화로 미룸).

  **구현**: `backend/paths.py`에 `DB_PATH = DATA_DIR / "companies.db"` 추가. `backend/storage.py`의 `save_company`/`load_company`/`list_companies`를 SQLite 기반으로 재작성 — `companies` 테이블(business_no PK + company_name/industry_name/credit_grade/status/parsed_at 조회용 중복 컬럼 + 전체 데이터를 담는 `data` JSON 컬럼), `save_company`는 `INSERT ... ON CONFLICT DO UPDATE`(UPSERT). **바로 위 사고의 교훈을 그대로 반영**: `_get_conn()`은 `DB_PATH`를 함수 "호출 시점"에 읽도록 구현(모듈 임포트 시점에 캡처하지 않음 — 코드 주석에도 사고 경위를 명시). `migrate_json_to_sqlite.py`(프로젝트 루트, `setup_tessdata.py`와 같은 컨벤션) 신규 — 기존 `data/*.json`을 새 DB로 이관하는 1회성 스크립트, **원본 JSON은 절대 자동 삭제하지 않음**(직전 사고 직후라 실데이터 다루는 스크립트는 항상 비파괴적으로 작성).

  **테스트**: 7개 파일(`test_storage.py` 7곳/`test_api.py`/`test_admin.py`/`test_master_list.py`/`test_category_list.py`/`test_mailing_list.py`)의 `monkeypatch.setattr(storage, "DATA_DIR", tmp_path)`를 `monkeypatch.setattr(storage, "DB_PATH", tmp_path / "test.db")`로 기계적 일괄 교체(로직 변경 없음, 각 테스트가 격리된 tmp_path를 쓰므로 DB 파일도 자연히 격리됨). 신규 테스트 1개(`test_save_company_upserts_same_business_no`) — 같은 사업자번호로 재저장 시 덮어쓰기만 되고 중복 행이 생기지 않는지 검증. 총 73개 테스트 통과.

  **실데이터 검증**: `python migrate_json_to_sqlite.py` 실행 → 기존 `data/*.json` 64개 전부 정상 이관 확인(`storage.list_companies()` 개수 일치). 서버 기동 후 대시보드(`total_companies: 64`) 정상, 옥산농원 상세 페이지(신용정보 카드 3개, 재무제표 상세 패널 3개 등) 마이그레이션 전과 동일하게 렌더링 확인(Playwright, 콘솔 에러 0건). 실제 PDF 재업로드(`35_성지에프앤디.pdf`)로 UPSERT 동작도 실사용 흐름에서 재확인(기존 사업자번호 411-81-84761이 새 `parsed_at`으로 갱신, 전체 개수는 64 유지 — 중복 행 생성 안 됨).

  **알려진 남은 제약**: 이번 작업은 로컬 저장소 안정성 개선이 목적이라, Render 무료 플랜(영구 디스크 없음)의 "재시작 시 데이터 초기화" 문제는 그대로다 — 별개 사안(영구 디스크/유료 플랜 전환 시 해결).

- 2026-08-18 (Claude): **"PDF 첨부 후 파싱부터 새롭게 해보고 싶은데, 데이터베이스 지워줄 수 있니?" → 데이터 초기화 + 법인형태별 4개 메뉴를 DB 기반으로 재설계.** 사용자가 SQLite 마이그레이션 직후 완전히 빈 상태에서 재테스트하고 싶다고 요청 — AskUserQuestion으로 범위 확인 후(DB만 vs DB+uploads+outputs 전부) **"전부"** 선택. `data/companies.db` 삭제(다음 실행 시 자동 재생성), `uploads/*.pdf` 65개·`outputs/*.xlsx` 65개 전부 삭제. **`data/*.json` 백업 64개는 의도적으로 보존**(마이그레이션 스크립트가 원본을 지우지 않았던 덕분에 남아있던 것 — 직전 실데이터 손실 사고 직후라 되돌릴 수 있는 안전장치를 스스로 없애지 않기 위해 사용자에게 확인 없이 임의로 지우지 않음).

  DB를 비운 뒤 사용자가 "영업 대상 분류(개인사업자/일반법인/농업회사법인/영농조합법인) 메뉴엔 왜 아직도 데이터가 보이냐"고 질문 — 그 4개 메뉴가 `companies.db`가 아니라 별개의 참고자료(`추가메뉴...xlsx`, 694행)를 읽는 화면이라 안 지워진 것이라고 설명했더니, 사용자가 **"이 메뉴는 파싱되어서 데이터베이스에 저장된 정보를 기반으로 조회조건에 따라 목록을 구성해주는 기능"**이라고 원래 의도를 정정 — 처음 이 기능을 만들 때(참고: 위 "법인형태별 4개 메뉴" 항목)는 사용자의 "엑셀 시트에 따라 목록을 제공해줘"라는 요청을 xlsx 그대로 노출하는 것으로 구현했었는데, 실제 의도는 xlsx가 아니라 **PDF 분석 결과(DB)를 법인형태로 분류해 보여주는 것**이었음이 뒤늦게 밝혀짐.

  **분류 기준 확정(AskUserQuestion)**: PDF의 `company_type` 필드는 "개인사업자"/"일반법인"/"외감"만 구분하고 농업회사법인·영농조합법인을 따로 못 가려서(실데이터 조사로 확인), (1) `company_type == "개인사업자"` → 개인사업자, (2) 회사명에 "농업회사법인" 포함 → 농업회사법인, (3) 회사명에 "영농조합법인" 포함 → 영농조합법인, (4) 나머지 전부 → 일반법인, 이 규칙으로 확정. 참고자료 xlsx는 이 4개 메뉴에서 완전히 제거하고 오로지 DB 데이터만 쓰기로 확정.

  **구현**: `backend/category_list.py`를 xlsx 로더에서 순수 분류 함수(`classify(company) -> str`)로 전면 교체. `backend/routers/category_list.py`는 `storage.list_companies()`를 분류 필터링하는 방식으로 재작성 — 모든 행이 이미 PDF로 분석된 회사이므로 이전의 "분석완료/미분석" 배지·"PDF 업로드" 유도 버튼 개념 자체가 사라짐(전부 상세보기 가능). `models.py`의 `CategoryListItem`을 단순화(analyzed/credit_grade_ref/revenue_ref/reg_no 제거, business_no/parsed_at 추가 — `CompanyListItem`과 형태 통일). `backend/excel/generator.py`의 `generate_category_excel`은 이제 xlsx 원본 컬럼 재수출이 아니라 Company 리스트를 받아 플랫 목록(No/사업자번호/기업체명/대표자명/업종/신용등급/매출액)으로 내보냄. `backend/routers/admin.py`에서 category_list 참고자료 업로드 대상 제거(더 이상 xlsx 자체가 없음 — master_list만 유지, 관련 신규 테스트로 404 확인). 프론트엔드 표 컬럼을 사업자번호/신용등급/매출액/재무진단으로 교체.

  **검증**: `tests/test_category_list.py` 전면 재작성(분류 로직 단위테스트 4개 포함, 가짜 xlsx 픽스처 완전히 제거) — 총 77개 테스트 통과. 데이터 초기화 후 실제 PDF 3건(옥산농원/삼진지에프/성지에프앤디) 재업로드 → 옥산농원(개인사업자)은 `individual` 메뉴에, 나머지 2건(일반법인 형태, 이름에 농업회사법인/영농조합법인 미포함)은 `general_corp` 메뉴에 정확히 분류됨을 API+Playwright로 확인, 상세보기 클릭 시 실제 상세 페이지로 정상 이동, 콘솔 에러 0건. 검증에 사용한 테스트 업로드 3건은 사용자가 원한 "완전히 빈 상태"를 지키기 위해 검증 직후 다시 삭제(uploads/outputs/DB 재초기화). 커밋 `e733c6f`.

- 2026-08-18 (Claude): **"파싱은 완료됐다는데 기업목록에서 500 에러" → master_list.py 방어 코드 추가.** 사용자가 데이터 초기화 후 재테스트하다가 재현 — 원인은 새 버그가 아니라 여전히 손상 상태인 `검색조회 목록.xlsx`("전체DB" 시트 없음, KeyError). 기존엔 파일이 아예 없는 경우만 빈 목록으로 방어했고, 파일은 있지만 기대한 시트가 없는 경우(손상/형식 다른 파일)는 못 잡고 있었음 — `load_master_rows()`에 `SHEET_NAME not in wb.sheetnames` 체크 추가해 500 대신 빈 목록 반환하도록 수정. 회귀 테스트 1개 추가, 총 78개 테스트 통과. 실제 로컬 서버로 재확인(더 이상 500 안 뜸). 커밋 `06aba9a`.

- 2026-08-18 (Claude): **"PDF가 업로드되서 파싱되고 데이터베이스에 저장되면 기업목록에 나와야 하는거 아니니?" → 기업목록을 xlsx 마스터리스트 방식에서 완전히 걷어내고 순수 DB 기반으로 되돌림.** 사용자의 지적이 정확했음 — 마스터리스트 방식(검색조회 목록.xlsx의 행을 기준으로 목록을 만들고 DB와 이름매칭)은 애초에 그 xlsx에 없는 회사는 PDF로 직접 분석해서 DB에 저장해도 기업목록에 절대 안 뜨는 구조적 결함이 있었음(파일 손상 이전부터 있던 문제 — 예: 옥산농원 자체가 원래 참고자료 xlsx 756개 목록에 없던 회사였음). AskUserQuestion으로 확인한 결과 사용자가 "단순하게 DB에 있는 회사만" 표시하는 쪽을 선택, 이어서 "제시해준 엑셀은 그냥 목록을 구성하기 위한 정보일 뿐 그 안에 있는 데이터는 아무 의미 없어"라고 확인해줘서 — 손상된 xlsx를 굳이 복구할 필요도 없어짐.

  **구현**: `backend/master_list.py`/`backend/routers/master_list.py` 삭제. `backend/routers/admin.py`도 삭제(참고자료 원격 업로드 엔드포인트의 유일한 남은 용도였던 master_list가 사라지면서 존재 이유가 없어짐 — category_list는 이미 앞선 커밋에서 DB 기반으로 전환돼 있었음). `models.py`에서 `MasterListItem`/`MasterList` 제거, `app.py`에서 두 라우터 등록 제거. 프론트엔드 기업목록 화면(`loadList`/`renderCompanyTable`/`populateIndustryOptions`/`exportAllExcel`)을 `/api/companies`(companies.py, 원래부터 있던 순수 DB 조회 엔드포인트, 손댄 적 없어 그대로 살아있었음) 기반으로 전면 복원 — 기업명/사업자번호/업종/매출액/영업이익/신용등급/재무진단/등록업데이트 컬럼.

  **검증**: `tests/test_master_list.py`/`test_admin.py` 삭제, 총 67개 테스트 통과. 실제 서버로 확인하던 중 **사용자가 이 세션 동안 직접 앱을 써서 실제 PDF 9건을 업로드해뒀던 것을 발견**(Claude의 테스트 데이터가 아니라 사용자 본인의 실사용 흔적 — 시각/타임스탬프로 확인) → 그 실데이터를 건드리지 않고 그대로 둔 채 검증 진행, Playwright로 기업목록에 총 10개(사용자 9개 + 확인용으로 추가한 옥산농원 1개) 즉시 표시됨을 확인, 콘솔 에러 0건. 커밋 `9ea60b8`.

- 2026-08-18 (Claude): **기업목록/영업 대상 분류 화면에 소소한 UX 개선 2건.** (1) "기업목록에서 기업명을 클릭하면 상세정보로 이동시켜줘" — 기업명 셀에 파란색+클릭 커서 스타일과 `viewDetail()` 핸들러 추가(기존 "상세" 버튼과 동일 동작). (2) 곧이어 "영업 대상 분류의 하위 메뉴들에도 동일하게 적용 + 목록에 엑셀 버튼도 보여야 해" — 4개 카테고리 메뉴 표에도 기업명 클릭 이동과 행별 "엑셀" 다운로드 버튼 추가. 두 커밋 모두 실제 서버(사용자가 실사용 중이던 인스턴스, 데이터는 건드리지 않고 그대로 활용)로 Playwright 검증, 콘솔 에러 0건. 커밋 `c8d67cb`, `c365e8d`.

- 2026-08-18 (Claude): **"'재무진단' 항목은 확실한 정보가 아닌 것 같다, 모든 목록에서 제거해줘" → 이어서 "기업목록과 영업 대상 분류를 완전히 통일시키고 검색 기능도 모두 넣어줘".** 사용자가 재무진단 컬럼(5축 중 최저 등급 하나만 보여주는 요약값)이 "확실한 정보가 아니다"라고 판단 — 목록 화면에서 완전히 제거하기로 결정. 이 작업 도중 두 목록 화면(기업목록 vs 영업 대상 분류)의 컬럼 구성이 애초에 서로 달랐다는 게 드러나, 사용자가 아예 두 화면을 완전히 동일하게 통일하고 영업 대상 분류에도 기업목록과 같은 검색/필터 기능을 전부 넣어달라고 확장 요청.

  **백엔드**: `storage.overall_diagnosis()`/`_DIAGNOSIS_RANK` 제거(다른 곳에서 전혀 안 쓰였음 — 상세 페이지의 "재무진단 요약"/"재무진단 분석의견" 섹션은 5축 원본 데이터(`company.diagnosis`)를 그대로 보여주는 별개 기능이라 영향 없음). `CompanyListItem.diagnosis_summary` 필드 삭제. **근본 원인 정리**: `backend/storage.py`에 `to_list_item()`/`filter_companies()` 공통 함수를 새로 만들어 `companies.py`와 `category_list.py`가 완전히 같은 로직을 공유하도록 함 — 기존엔 두 라우터가 각자 따로 목록 항목을 조립하고 있어서 항목 구성이 자연스럽게 어긋나 있었던 것이 이번 "통일해달라" 요청의 근본 원인이었음. `CategoryListItem` 모델 자체를 없애고 `CategoryList.items`가 `CompanyListItem`을 그대로 씀. `category_list` 라우터가 `companies` 라우터와 동일한 필터 파라미터(q/industry/grade_band/revenue_min/revenue_max) 전부 지원하도록 확장. 전용 카테고리 엑셀 생성 엔드포인트(`generate_category_excel`)는 제거하고, 기업목록과 똑같이 "필터링된 목록을 순회하며 개별 기업종합보고서를 하나씩 내려받는" 방식으로 통일.

  **프론트엔드**: `renderCompanyTable`/`renderCategoryTable` 두 함수를 `renderCompanyRows(tbodyId, items)` 하나로 합쳐 두 화면이 진짜로 동일한 렌더링 코드를 쓰게 함. `populateIndustryOptions`를 select 엘리먼트 id로 매개변수화해 두 화면에서 재사용. 영업 대상 분류 화면에 업종/신용등급/매출액 필터 드롭다운 신설(기업목록과 동일한 옵션), "엑셀 다운로드"(단일 요약 파일) 버튼을 기업목록과 같은 "전체 엑셀 내보내기"(개별 보고서 순차 다운로드)로 교체.

  **검증**: `tests/test_category_list.py`에 필터 3종(업종/신용등급/매출액) + 두 화면 항목 구성이 완전히 동일한지 확인하는 테스트 추가, 기존 엑셀 export 테스트는 해당 엔드포인트 제거로 삭제 — 총 70개 테스트 통과. 사용자가 실사용 중이던 서버(실제 회사 10건)를 재시작해 새 코드 반영 후 Playwright로 두 화면의 표 헤더가 문자 그대로 동일함을 확인(`기업명/사업자번호/업종/매출액(백만)/영업이익(백만)/신용등급/등록업데이트`), 필터 드롭다운·업종 옵션 로드·검색·기업명 클릭 이동·엑셀 버튼 전부 정상 동작, 콘솔 에러 0건. 커밋 `32bec6f`.

- 2026-08-18 (Claude): **Render 배포에서 로그인 게이트 제거 + DB 마이그레이션 반영 확인.** 사용자가 "친구랑 나만 볼 거고 민감한 정보 없다"며 배포 URL에서 아이디/비밀번호 입력을 빼달라고 요청 — Render API로 `APP_LOGIN_PASSWORD`/`APP_LOGIN_USER` 환경변수 삭제(더불어 이미 죽은 `ADMIN_UPLOAD_TOKEN`도 함께 정리), 강제 재배포로 반영(이번에도 env var 변경만으로는 안 되고 재배포가 필요했음 — 이전 세션에서 배운 대로). 이 재배포가 마침 최신 커밋(SQLite 마이그레이션·화면 통일 등 로컬에서만 진행되고 Render엔 아직 안 올라가 있던 변경사항 전부)을 함께 반영하는 계기도 됨. 사용자가 "DB로 관리하는 변경사항도 잘 반영된거지?"라고 재확인 요청 — `/api/companies`, `/api/category-list/individual`, `/api/dashboard/summary` 전부 200 정상 응답, SQLite 초기화도 정상임을 curl로 확인해 답변. **알아둘 점**: Render 인스턴스는 로컬과 완전히 별개의 SQLite DB를 쓰므로, 로컬에 있는 데이터가 자동으로 Render에 동기화되지 않음 — 공유하려면 Render 쪽에 PDF를 별도로 다시 올려야 하고, 무료 플랜이라 재시작/재배포마다 또 초기화된다는 제약은 여전함(이미 여러 번 고지한 사항, 사용자도 인지하고 진행).

- 2026-08-18 (Claude): **"기업 상세화면 최하단에 '기업정보 삭제' 버튼 만들어줘, DB에서 지워지고 나중에 다시 올릴 수 있도록" — 삭제 기능 신규 구현.** `backend/storage.py`에 `delete_company(business_no) -> bool`(SQL `DELETE FROM companies WHERE business_no = ?`, 존재 여부를 rowcount로 판정) 추가 — 원본 PDF(`uploads/`)는 안 건드리므로 재업로드하면 그대로 재등록됨(요청사항 그대로). `DELETE /api/companies/{business_no}` 엔드포인트(204/404) 신규. 상세 페이지 최하단에 빨간 "위험 구역" 섹션 + 확인 다이얼로그를 거치는 삭제 버튼 추가, 성공 시 기업목록으로 자동 이동.

  **검증**: `tests/test_storage.py` 2개(삭제 후 재업로드로 복구되는지, 존재하지 않는 사업자번호는 False), `tests/test_api.py` 2개(204 성공/404 미존재) 신규 — 총 74개 테스트 통과. 사용자가 실사용 중이던 서버(실제 회사 60여 건 누적, 절대 안 건드림)에서, 그와 무관한 테스트용 PDF(옥산농원 샘플) 1건만 별도로 업로드해 업로드→상세페이지 삭제버튼 클릭→확인 다이얼로그→삭제 완료 알림→기업목록 이동까지 Playwright로 실제 흐름 검증, 삭제 후 API로 404 재확인, 콘솔 에러 0건. 커밋 `c90acda`. (Render 배포에는 아직 반영 안 함 — 필요시 재배포 요청 대기 중.)
