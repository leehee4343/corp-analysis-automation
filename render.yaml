services:
  - type: web
    name: corp-analysis-automation
    env: docker
    dockerfilePath: ./Dockerfile
    plan: starter
    disk:
      name: app-data
      mountPath: /app/storage
      sizeGB: 1
    envVars:
      - key: APP_STORAGE_ROOT
        value: /app/storage
